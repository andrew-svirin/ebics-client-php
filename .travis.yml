os: linux
dist: xenial
language: php
php:
  - '7.4'
cache:
  directories:
    - vendor
before_script:
  - composer update --dev --no-interaction
script:
  - vendor/bin/phpstan analyse
  - vendor/bin/phpunit
  - vendor/bin/phpcs